<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".RecyclerView_sanpham.ConfigNotifcation"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASM_Adr2_Hieunnph32561"
        tools:targetApi="31">
        <activity android:name=".RecyclerView_sanpham.Gioi_thieu" />
        <activity
            android:name=".RecyclerView_sanpham.Setting"
            android:exported="false" />
        <activity
            android:name=".Login_register.Dangky"
            android:exported="false" />
        <activity
            android:name=".RecyclerView_sanpham.MainActivity"
            android:exported="false" />
        <activity
            android:name=".Login_register.Dangnhap"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>